language: node_js

node_js:
  - 10

cache:
  directories:
    - ~/.npm

install:
  - npm ci

before_deploy:
- npm version patch

deploy:
  provider: script
  skip_cleanup: true
  script: .travis/pu.sh
  on:
    branch: cleanup
    tags: false
